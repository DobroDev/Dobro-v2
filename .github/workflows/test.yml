name: 'test'
  on: # rebuild any PRs and main branch changes
  push:
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run build
